import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maestroapps.gtl',
  appName: 'gtl',
  webDir: 'www',
  bundledWebRuntime: false,
  
};

export default config;
